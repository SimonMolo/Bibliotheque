{% extends 'base.html.twig' %}
{% block title%}
    {{ livre.title }}
{% endblock title %}

{% block main %}
    <body>
    <article class="flex articleLivre border">
       {#  <div>
            <img class="coverImg" src="{{ livre.img }}" alt="{{ livre.title }}">
        </div>#}
        <div class="border infoLivre">
              <h1 class="center">{{livre.title}}</h1>
              <p class="center"> {{ livre.author}}</p>
              <em class="center date">Publié le:{{ livre.Date|date('d/m/y')}}</em>
        </div>
        {% if book.author %}
            <h3>{{ book.author.firstName }} {{ book.author.lastName }}</h3>
        {% endif %}
    </article>
    <div class="border synopsis">
        <h2>synopsis du livre: {{ livre.title }}</h2>
        <p>{{ livre.nbPages }}</p>
    </div>
    </body>
{% endblock main %}